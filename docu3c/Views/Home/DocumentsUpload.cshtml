@model  docu3c.DocumentUpload
@{
    ViewBag.Title = "Upload Documents";
}
<h5>

    @if (Session["UserName"] != null)
    {
        <text>
            Hi <b>@Session["UserName"].ToString() </b>,<br />

        </text>

    }

</h5>
 Let's create a portfolio to store all your customer's documents.
<div class="pagesubtitle">
    <b>  Portfolio Name: </b> @Model.PortfolioName
</div>

@using (Html.BeginForm("DocumentsUpload", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal" id="upload">

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.files, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.files, "", new { @type = "file", @multiple = "multiple" })
                @Html.ValidationMessageFor(model => model.files, "", new { @class = "text-danger" })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Upload" class="btn btn-primary" id="btnUpdate" />
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10 text-success">
                @ViewBag.UploadStatus
            </div>
        </div>

    </div>
    <div id="MLButton">
        <input type="submit" value="Parse" class="btn btn-primary" />
        <input type="submit" value="classify" class="btn btn-primary" />

    </div>
}
<script src="~/Scripts/jquery-3.3.1.min.js"></script>
<script>
     alert("Hi");
    $(document).ready(function () {
        //$("#MLButton").hide();
        alert("Hi2");
        @*if (@ViewBag.UploadClicked= 1) { $("#upload").hide();
            $("#MLButton").show();}
        $("#btnUpdate").click(function () {
            //  alert("button"); // Remove this line if it worked
            $("#upload").hide();
            $("#MLButton").show();
        });*@
    });
</script>
